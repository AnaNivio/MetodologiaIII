class ClientRepository:
    def __init__(self)
        self._id = 1
        self._client_array = {}

    def save(self,client)
        self._client_array[self.id] = client
        self._id = self._id + 1

    def get(self)

        for i in range(len(self._client_array)){
            print self._client_array(i.has_key,0)         
        }
