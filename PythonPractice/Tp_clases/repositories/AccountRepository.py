class AccountRepository:
    def __init__(self)
        self._id = 1
        self._account_array = {}

    def save(self,account)
        self._account_array[self.id] = account
        self._id = self._id + 1

        def get(self)

        for i in range(len(self._account_array)){
            print self._account_array(i.has_key,0)         
        }